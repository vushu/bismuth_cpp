#pragma once
#include "scene.hpp"
class MainMenuScene : public Scene {
    public:
        // constructors, asssignment, destructor
        MainMenuScene ();
        ~MainMenuScene ();
        void update(float dt);

};
