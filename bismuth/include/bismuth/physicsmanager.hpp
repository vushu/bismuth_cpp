#pragma once
namespace bi {
    class PhysicsManager{
        public:
            // constructors, asssignment, destructor
            PhysicsManager();
            ~PhysicsManager();
            void addPhysicObject();

        private:

    };
}
