class PlayerSystem{
    public:
        // constructors, asssignment, destructor
        PlayerSystem();
        PlayerSystem(const PlayerSystem&);
        PlayerSystem& operator=(const PlayerSystem&);
        ~PlayerSystem();

    private:

};
